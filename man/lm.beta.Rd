% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lm.beta.R
\name{lm.beta}
\alias{lm.beta}
\title{Fit Linear Model with Standardized Variables}
\usage{
lm.beta(formula, data, center = TRUE, scale = TRUE, return_data = FALSE)
}
\arguments{
\item{formula}{A model formula (e.g., y ~ x1 + x2)}

\item{data}{A data.frame containing the variables in the formula}

\item{center}{Logical; whether to center variables (default = TRUE)}

\item{scale}{Logical; whether to scale variables (default = TRUE)}

\item{return_data}{Logical; return standardized data as attribute (default = FALSE)}
}
\value{
An object of class `lm`. If `return_data = TRUE`, the standardized
data are attached as an attribute `"scaled_data"`.
}
\description{
Fits a linear model (`lm`) using standardized (z-scored) numeric variables
from the supplied formula. Standardization is performed within the function.
}
\details{
Only numeric variables in the formula are standardized. Factors are left unchanged.
}
\examples{
df <- data.frame(
  y = rnorm(100),
  x1 = rnorm(100),
  x2 = rnorm(100),
  group = factor(sample(letters[1:2], 100, TRUE))
)

model <- lm.beta(y ~ x1 + x2 + group, data = df)
summary(model)

}
